# socket.io-prometheus-tracker/examples/express

A simple example of using the library as part of a Socket.IO/Express
application.

# Running

`npm run start`

# Accessing metrics

`curl localhost:3000/metrics`
